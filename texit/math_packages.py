from texit.base import BaseElement


class Urcorner(BaseElement):
    """⌝  related to `amsfonts mathclose` category."""

    ...


class Lrcorner(BaseElement):
    """⌟  related to `amsfonts mathclose` category."""

    ...


class Ulcorner(BaseElement):
    """⌜  related to `amsfonts mathopen` category."""

    ...


class Llcorner(BaseElement):
    """⌞  related to `amsfonts mathopen` category."""

    ...


class Yen(BaseElement):
    """¥  related to `amsfonts mathord` category."""

    ...


class Circledr(BaseElement):
    """®  related to `amsfonts mathord` category."""

    ...


class Mho(BaseElement):
    """℧  related to `amsfonts mathord` category."""

    ...


class Dashleftarrow(BaseElement):
    """⇠  related to `amsfonts mathord` category."""

    ...


class Dashrightarrow(BaseElement):
    """⇢  related to `amsfonts mathord` category."""

    ...


class Checkmark(BaseElement):
    """✓  related to `amsfonts mathord` category."""

    ...


class Maltese(BaseElement):
    """✠  related to `amsfonts mathord` category."""

    ...


class Sqsubset(BaseElement):
    """⊏  related to `amsfonts mathrel` category."""

    ...


class Sqsupset(BaseElement):
    """⊐  related to `amsfonts mathrel` category."""

    ...


class Dddot(BaseElement):
    """⃛  related to `amsmath mathaccent` category."""

    ...


class Ddddot(BaseElement):
    """⃜  related to `amsmath mathaccent` category."""

    ...


class Overleftrightarrow(BaseElement):
    """⃡  related to `amsmath mathaccent` category."""

    ...


class Underleftarrow(BaseElement):
    """⃮  related to `amsmath mathaccent` category."""

    ...


class Underrightarrow(BaseElement):
    """⃯  related to `amsmath mathaccent` category."""

    ...


class Iint(BaseElement):
    """∬  related to `amsmath mathop` category."""

    ...


class Iiint(BaseElement):
    """∭  related to `amsmath mathop` category."""

    ...


class Iiiint(BaseElement):
    """⨌  related to `amsmath mathop` category."""

    ...


class Mathring(BaseElement):
    """̊  related to `amssymb mathaccent` category."""

    ...


class Eth(BaseElement):
    """ð  related to `amssymb mathalpha` category."""

    ...


class Digamma(BaseElement):
    """Ϝ  related to `amssymb mathalpha` category."""

    ...


class Hslash(BaseElement):
    """ℏ  related to `amssymb mathalpha` category."""

    ...


class Wp(BaseElement):
    """℘  related to `amssymb mathalpha` category."""

    ...


class Beth(BaseElement):
    """ℶ  related to `amssymb mathalpha` category."""

    ...


class Gimel(BaseElement):
    """ℷ  related to `amssymb mathalpha` category."""

    ...


class Daleth(BaseElement):
    """ℸ  related to `amssymb mathalpha` category."""

    ...


class Varkappa(BaseElement):
    """𝜘  related to `amssymb mathalpha` category."""

    ...


class Dotplus(BaseElement):
    """∔  related to `amssymb mathbin` category."""

    ...


class Smallsetminus(BaseElement):
    """∖  related to `amssymb mathbin` category."""

    ...


class Wedge(BaseElement):
    """∧  related to `amssymb mathbin` category."""

    ...


class Wr(BaseElement):
    """≀  related to `amssymb mathbin` category."""

    ...


class Circledcirc(BaseElement):
    """⊚  related to `amssymb mathbin` category."""

    ...


class Circledast(BaseElement):
    """⊛  related to `amssymb mathbin` category."""

    ...


class Circleddash(BaseElement):
    """⊝  related to `amssymb mathbin` category."""

    ...


class Boxplus(BaseElement):
    """⊞  related to `amssymb mathbin` category."""

    ...


class Boxminus(BaseElement):
    """⊟  related to `amssymb mathbin` category."""

    ...


class Boxtimes(BaseElement):
    """⊠  related to `amssymb mathbin` category."""

    ...


class Boxdot(BaseElement):
    """⊡  related to `amssymb mathbin` category."""

    ...


class Intercal(BaseElement):
    """⊺  related to `amssymb mathbin` category."""

    ...


class Veebar(BaseElement):
    """⊻  related to `amssymb mathbin` category."""

    ...


class Barwedge(BaseElement):
    """⊼  related to `amssymb mathbin` category."""

    ...


class Divideontimes(BaseElement):
    """⋇  related to `amssymb mathbin` category."""

    ...


class Ltimes(BaseElement):
    """⋉  related to `amssymb mathbin` category."""

    ...


class Rtimes(BaseElement):
    """⋊  related to `amssymb mathbin` category."""

    ...


class Leftthreetimes(BaseElement):
    """⋋  related to `amssymb mathbin` category."""

    ...


class Rightthreetimes(BaseElement):
    """⋌  related to `amssymb mathbin` category."""

    ...


class Curlyvee(BaseElement):
    """⋎  related to `amssymb mathbin` category."""

    ...


class Curlywedge(BaseElement):
    """⋏  related to `amssymb mathbin` category."""

    ...


class Cap(BaseElement):
    """⋒  related to `amssymb mathbin` category."""

    ...


class Cup(BaseElement):
    """⋓  related to `amssymb mathbin` category."""

    ...


class Rhd(BaseElement):
    """▷  related to `amssymb mathbin` category."""

    ...


class Lhd(BaseElement):
    """◁  related to `amssymb mathbin` category."""

    ...


class Blacklozenge(BaseElement):
    """⧫  related to `amssymb mathbin` category."""

    ...


class Doublebarwedge(BaseElement):
    """⩞  related to `amssymb mathbin` category."""

    ...


class Join(BaseElement):
    """⨝  related to `amssymb mathop` category."""

    ...


class Backepsilon(BaseElement):
    """϶  related to `amssymb mathord` category."""

    ...


class Backprime(BaseElement):
    """‵  related to `amssymb mathord` category."""

    ...


class Finv(BaseElement):
    """Ⅎ  related to `amssymb mathord` category."""

    ...


class Circlearrowleft(BaseElement):
    """↺  related to `amssymb mathord` category."""

    ...


class Circlearrowright(BaseElement):
    """↻  related to `amssymb mathord` category."""

    ...


class Complement(BaseElement):
    """∁  related to `amssymb mathord` category."""

    ...


class Nexists(BaseElement):
    """∄  related to `amssymb mathord` category."""

    ...


class Varnothing(BaseElement):
    """∅  related to `amssymb mathord` category."""

    ...


class Measuredangle(BaseElement):
    """∡  related to `amssymb mathord` category."""

    ...


class Sphericalangle(BaseElement):
    """∢  related to `amssymb mathord` category."""

    ...


class Therefore(BaseElement):
    """∴  related to `amssymb mathord` category."""

    ...


class Because(BaseElement):
    """∵  related to `amssymb mathord` category."""

    ...


class Diamond(BaseElement):
    """◇  related to `amssymb mathord` category."""

    ...


class Lozenge(BaseElement):
    """◊  related to `amssymb mathord` category."""

    ...


class Square(BaseElement):
    """◻  related to `amssymb mathord` category."""

    ...


class Blacksquare(BaseElement):
    """◼  related to `amssymb mathord` category."""

    ...


class Bigstar(BaseElement):
    """★  related to `amssymb mathord` category."""

    ...


class Blacksquare(BaseElement):
    """⬛  related to `amssymb mathord` category."""

    ...


class Square(BaseElement):
    """⬜  related to `amssymb mathord` category."""

    ...


class Nwarrow(BaseElement):
    """↖  related to `amssymb mathrel` category."""

    ...


class Nleftarrow(BaseElement):
    """↚  related to `amssymb mathrel` category."""

    ...


class Nrightarrow(BaseElement):
    """↛  related to `amssymb mathrel` category."""

    ...


class Twoheadleftarrow(BaseElement):
    """↞  related to `amssymb mathrel` category."""

    ...


class Twoheadrightarrow(BaseElement):
    """↠  related to `amssymb mathrel` category."""

    ...


class Leftarrowtail(BaseElement):
    """↢  related to `amssymb mathrel` category."""

    ...


class Rightarrowtail(BaseElement):
    """↣  related to `amssymb mathrel` category."""

    ...


class Looparrowleft(BaseElement):
    """↫  related to `amssymb mathrel` category."""

    ...


class Looparrowright(BaseElement):
    """↬  related to `amssymb mathrel` category."""

    ...


class Leftrightsquigarrow(BaseElement):
    """↭  related to `amssymb mathrel` category."""

    ...


class Nleftrightarrow(BaseElement):
    """↮  related to `amssymb mathrel` category."""

    ...


class Lsh(BaseElement):
    """↰  related to `amssymb mathrel` category."""

    ...


class Rsh(BaseElement):
    """↱  related to `amssymb mathrel` category."""

    ...


class Curvearrowleft(BaseElement):
    """↶  related to `amssymb mathrel` category."""

    ...


class Curvearrowright(BaseElement):
    """↷  related to `amssymb mathrel` category."""

    ...


class Upharpoonright(BaseElement):
    """↾  related to `amssymb mathrel` category."""

    ...


class Upharpoonleft(BaseElement):
    """↿  related to `amssymb mathrel` category."""

    ...


class Downharpoonright(BaseElement):
    """⇂  related to `amssymb mathrel` category."""

    ...


class Downharpoonleft(BaseElement):
    """⇃  related to `amssymb mathrel` category."""

    ...


class Rightleftarrows(BaseElement):
    """⇄  related to `amssymb mathrel` category."""

    ...


class Leftrightarrows(BaseElement):
    """⇆  related to `amssymb mathrel` category."""

    ...


class Leftleftarrows(BaseElement):
    """⇇  related to `amssymb mathrel` category."""

    ...


class Upuparrows(BaseElement):
    """⇈  related to `amssymb mathrel` category."""

    ...


class Rightrightarrows(BaseElement):
    """⇉  related to `amssymb mathrel` category."""

    ...


class Downdownarrows(BaseElement):
    """⇊  related to `amssymb mathrel` category."""

    ...


class Leftrightharpoons(BaseElement):
    """⇋  related to `amssymb mathrel` category."""

    ...


class Nleftarrow(BaseElement):
    """⇍  related to `amssymb mathrel` category."""

    ...


class Nleftrightarrow(BaseElement):
    """⇎  related to `amssymb mathrel` category."""

    ...


class Nrightarrow(BaseElement):
    """⇏  related to `amssymb mathrel` category."""

    ...


class Lleftarrow(BaseElement):
    """⇚  related to `amssymb mathrel` category."""

    ...


class Rrightarrow(BaseElement):
    """⇛  related to `amssymb mathrel` category."""

    ...


class Rightsquigarrow(BaseElement):
    """⇝  related to `amssymb mathrel` category."""

    ...


class Nmid(BaseElement):
    """∤  related to `amssymb mathrel` category."""

    ...


class Nparallel(BaseElement):
    """∦  related to `amssymb mathrel` category."""

    ...


class Backsim(BaseElement):
    """∽  related to `amssymb mathrel` category."""

    ...


class Nsim(BaseElement):
    """≁  related to `amssymb mathrel` category."""

    ...


class Eqsim(BaseElement):
    """≂  related to `amssymb mathrel` category."""

    ...


class Ncong(BaseElement):
    """≇  related to `amssymb mathrel` category."""

    ...


class Approxeq(BaseElement):
    """≊  related to `amssymb mathrel` category."""

    ...


class Bumpeq(BaseElement):
    """≎  related to `amssymb mathrel` category."""

    ...


class Bumpeq(BaseElement):
    """≏  related to `amssymb mathrel` category."""

    ...


class Doteq(BaseElement):
    """≑  related to `amssymb mathrel` category."""

    ...


class Fallingdotseq(BaseElement):
    """≒  related to `amssymb mathrel` category."""

    ...


class Risingdotseq(BaseElement):
    """≓  related to `amssymb mathrel` category."""

    ...


class Eqcirc(BaseElement):
    """≖  related to `amssymb mathrel` category."""

    ...


class Circeq(BaseElement):
    """≗  related to `amssymb mathrel` category."""

    ...


class Triangleq(BaseElement):
    """≜  related to `amssymb mathrel` category."""

    ...


class Leqq(BaseElement):
    """≦  related to `amssymb mathrel` category."""

    ...


class Geqq(BaseElement):
    """≧  related to `amssymb mathrel` category."""

    ...


class Lneqq(BaseElement):
    """≨  related to `amssymb mathrel` category."""

    ...


class Gneqq(BaseElement):
    """≩  related to `amssymb mathrel` category."""

    ...


class Between(BaseElement):
    """≬  related to `amssymb mathrel` category."""

    ...


class Nless(BaseElement):
    """≮  related to `amssymb mathrel` category."""

    ...


class Ngtr(BaseElement):
    """≯  related to `amssymb mathrel` category."""

    ...


class Nleq(BaseElement):
    """≰  related to `amssymb mathrel` category."""

    ...


class Ngeq(BaseElement):
    """≱  related to `amssymb mathrel` category."""

    ...


class Lesssim(BaseElement):
    """≲  related to `amssymb mathrel` category."""

    ...


class Gtrsim(BaseElement):
    """≳  related to `amssymb mathrel` category."""

    ...


class Lessgtr(BaseElement):
    """≶  related to `amssymb mathrel` category."""

    ...


class Gtrless(BaseElement):
    """≷  related to `amssymb mathrel` category."""

    ...


class Preccurlyeq(BaseElement):
    """≼  related to `amssymb mathrel` category."""

    ...


class Succcurlyeq(BaseElement):
    """≽  related to `amssymb mathrel` category."""

    ...


class Precsim(BaseElement):
    """≾  related to `amssymb mathrel` category."""

    ...


class Succsim(BaseElement):
    """≿  related to `amssymb mathrel` category."""

    ...


class Nprec(BaseElement):
    """⊀  related to `amssymb mathrel` category."""

    ...


class Nsucc(BaseElement):
    """⊁  related to `amssymb mathrel` category."""

    ...


class Nsubseteq(BaseElement):
    """⊈  related to `amssymb mathrel` category."""

    ...


class Nsupseteq(BaseElement):
    """⊉  related to `amssymb mathrel` category."""

    ...


class Subsetneq(BaseElement):
    """⊊  related to `amssymb mathrel` category."""

    ...


class Supsetneq(BaseElement):
    """⊋  related to `amssymb mathrel` category."""

    ...


class Dashv(BaseElement):
    """⊣  related to `amssymb mathrel` category."""

    ...


class Vdash(BaseElement):
    """⊨  related to `amssymb mathrel` category."""

    ...


class Vdash(BaseElement):
    """⊩  related to `amssymb mathrel` category."""

    ...


class Vvdash(BaseElement):
    """⊪  related to `amssymb mathrel` category."""

    ...


class Nvdash(BaseElement):
    """⊬  related to `amssymb mathrel` category."""

    ...


class Nvdash(BaseElement):
    """⊭  related to `amssymb mathrel` category."""

    ...


class Nvdash(BaseElement):
    """⊮  related to `amssymb mathrel` category."""

    ...


class Nvdash(BaseElement):
    """⊯  related to `amssymb mathrel` category."""

    ...


class Vartriangleleft(BaseElement):
    """⊲  related to `amssymb mathrel` category."""

    ...


class Vartriangleright(BaseElement):
    """⊳  related to `amssymb mathrel` category."""

    ...


class Trianglelefteq(BaseElement):
    """⊴  related to `amssymb mathrel` category."""

    ...


class Trianglerighteq(BaseElement):
    """⊵  related to `amssymb mathrel` category."""

    ...


class Multimap(BaseElement):
    """⊸  related to `amssymb mathrel` category."""

    ...


class Backsimeq(BaseElement):
    """⋍  related to `amssymb mathrel` category."""

    ...


class Subset(BaseElement):
    """⋐  related to `amssymb mathrel` category."""

    ...


class Supset(BaseElement):
    """⋑  related to `amssymb mathrel` category."""

    ...


class Pitchfork(BaseElement):
    """⋔  related to `amssymb mathrel` category."""

    ...


class Lessdot(BaseElement):
    """⋖  related to `amssymb mathrel` category."""

    ...


class Gtrdot(BaseElement):
    """⋗  related to `amssymb mathrel` category."""

    ...


class Lll(BaseElement):
    """⋘  related to `amssymb mathrel` category."""

    ...


class Ggg(BaseElement):
    """⋙  related to `amssymb mathrel` category."""

    ...


class Lesseqgtr(BaseElement):
    """⋚  related to `amssymb mathrel` category."""

    ...


class Gtreqless(BaseElement):
    """⋛  related to `amssymb mathrel` category."""

    ...


class Curlyeqprec(BaseElement):
    """⋞  related to `amssymb mathrel` category."""

    ...


class Curlyeqsucc(BaseElement):
    """⋟  related to `amssymb mathrel` category."""

    ...


class Npreceq(BaseElement):
    """⋠  related to `amssymb mathrel` category."""

    ...


class Nsucceq(BaseElement):
    """⋡  related to `amssymb mathrel` category."""

    ...


class Lnsim(BaseElement):
    """⋦  related to `amssymb mathrel` category."""

    ...


class Gnsim(BaseElement):
    """⋧  related to `amssymb mathrel` category."""

    ...


class Precnsim(BaseElement):
    """⋨  related to `amssymb mathrel` category."""

    ...


class Succnsim(BaseElement):
    """⋩  related to `amssymb mathrel` category."""

    ...


class Ntriangleleft(BaseElement):
    """⋪  related to `amssymb mathrel` category."""

    ...


class Ntriangleright(BaseElement):
    """⋫  related to `amssymb mathrel` category."""

    ...


class Ntrianglelefteq(BaseElement):
    """⋬  related to `amssymb mathrel` category."""

    ...


class Ntrianglerighteq(BaseElement):
    """⋭  related to `amssymb mathrel` category."""

    ...


class Leqslant(BaseElement):
    """⩽  related to `amssymb mathrel` category."""

    ...


class Geqslant(BaseElement):
    """⩾  related to `amssymb mathrel` category."""

    ...


class Lessapprox(BaseElement):
    """⪅  related to `amssymb mathrel` category."""

    ...


class Gtrapprox(BaseElement):
    """⪆  related to `amssymb mathrel` category."""

    ...


class Lneq(BaseElement):
    """⪇  related to `amssymb mathrel` category."""

    ...


class Gneq(BaseElement):
    """⪈  related to `amssymb mathrel` category."""

    ...


class Lnapprox(BaseElement):
    """⪉  related to `amssymb mathrel` category."""

    ...


class Gnapprox(BaseElement):
    """⪊  related to `amssymb mathrel` category."""

    ...


class Lesseqqgtr(BaseElement):
    """⪋  related to `amssymb mathrel` category."""

    ...


class Gtreqqless(BaseElement):
    """⪌  related to `amssymb mathrel` category."""

    ...


class Eqslantless(BaseElement):
    """⪕  related to `amssymb mathrel` category."""

    ...


class Eqslantgtr(BaseElement):
    """⪖  related to `amssymb mathrel` category."""

    ...


class Precapprox(BaseElement):
    """⪷  related to `amssymb mathrel` category."""

    ...


class Succapprox(BaseElement):
    """⪸  related to `amssymb mathrel` category."""

    ...


class Precnapprox(BaseElement):
    """⪹  related to `amssymb mathrel` category."""

    ...


class Succnapprox(BaseElement):
    """⪺  related to `amssymb mathrel` category."""

    ...


class Subseteqq(BaseElement):
    """⫅  related to `amssymb mathrel` category."""

    ...


class Supseteqq(BaseElement):
    """⫆  related to `amssymb mathrel` category."""

    ...


class Subsetneqq(BaseElement):
    """⫋  related to `amssymb mathrel` category."""

    ...


class Supsetneqq(BaseElement):
    """⫌  related to `amssymb mathrel` category."""

    ...


class Sptilde(BaseElement):
    """~  related to `amsxtra mathord` category."""

    ...


class Spddot(BaseElement):
    """¨  related to `amsxtra mathord` category."""

    ...


class Eth(BaseElement):
    """ð  related to `arevmath mathalpha` category."""

    ...


class Varbeta(BaseElement):
    """ϐ  related to `arevmath mathalpha` category."""

    ...


class Stigma(BaseElement):
    """Ϛ  related to `arevmath mathalpha` category."""

    ...


class Stigma(BaseElement):
    """ϛ  related to `arevmath mathalpha` category."""

    ...


class Digamma(BaseElement):
    """ϝ  related to `arevmath mathalpha` category."""

    ...


class Koppa(BaseElement):
    """Ϟ  related to `arevmath mathalpha` category."""

    ...


class Koppa(BaseElement):
    """ϟ  related to `arevmath mathalpha` category."""

    ...


class Sampi(BaseElement):
    """Ϡ  related to `arevmath mathalpha` category."""

    ...


class Sampi(BaseElement):
    """ϡ  related to `arevmath mathalpha` category."""

    ...


class Hslash(BaseElement):
    """ℏ  related to `arevmath mathalpha` category."""

    ...


class Qoppa(BaseElement):
    """Ϙ  related to `arevmath mathord` category."""

    ...


class Qoppa(BaseElement):
    """ϙ  related to `arevmath mathord` category."""

    ...


class Mho(BaseElement):
    """℧  related to `arevmath mathord` category."""

    ...


class Steaming(BaseElement):
    """☕  related to `arevmath mathord` category."""

    ...


class Pointright(BaseElement):
    """☞  related to `arevmath mathord` category."""

    ...


class Skull(BaseElement):
    """☠  related to `arevmath mathord` category."""

    ...


class Radiation(BaseElement):
    """☢  related to `arevmath mathord` category."""

    ...


class Biohazard(BaseElement):
    """☣  related to `arevmath mathord` category."""

    ...


class Yinyang(BaseElement):
    """☯  related to `arevmath mathord` category."""

    ...


class Quarternote(BaseElement):
    """♩  related to `arevmath mathord` category."""

    ...


class Eighthnote(BaseElement):
    """♪  related to `arevmath mathord` category."""

    ...


class Sixteenthnote(BaseElement):
    """♬  related to `arevmath mathord` category."""

    ...


class Recycle(BaseElement):
    """♻  related to `arevmath mathord` category."""

    ...


class Anchor(BaseElement):
    """⚓  related to `arevmath mathord` category."""

    ...


class Swords(BaseElement):
    """⚔  related to `arevmath mathord` category."""

    ...


class Warning(BaseElement):
    """⚠  related to `arevmath mathord` category."""

    ...


class Pencil(BaseElement):
    """✎  related to `arevmath mathord` category."""

    ...


class Ballotx(BaseElement):
    """✗  related to `arevmath mathord` category."""

    ...


class Arrowbullet(BaseElement):
    """➢  related to `arevmath mathord` category."""

    ...


class Iint(BaseElement):
    """∬  related to `esint mathop` category."""

    ...


class Iiint(BaseElement):
    """∭  related to `esint mathop` category."""

    ...


class Oiint(BaseElement):
    """∯  related to `esint mathop` category."""

    ...


class Varointclockwise(BaseElement):
    """∲  related to `esint mathop` category."""

    ...


class Ointctrclockwise(BaseElement):
    """∳  related to `esint mathop` category."""

    ...


class Iiiint(BaseElement):
    """⨌  related to `esint mathop` category."""

    ...


class Fint(BaseElement):
    """⨏  related to `esint mathop` category."""

    ...


class Sqint(BaseElement):
    """⨖  related to `esint mathop` category."""

    ...


class Hslash(BaseElement):
    """ℏ  related to `fourier mathalpha` category."""

    ...


class Smallsetminus(BaseElement):
    """∖  related to `fourier mathbin` category."""

    ...


class Intercal(BaseElement):
    """⊺  related to `fourier mathbin` category."""

    ...


class Blacktriangleright(BaseElement):
    """▸  related to `fourier mathbin` category."""

    ...


class Blacktriangleleft(BaseElement):
    """◂  related to `fourier mathbin` category."""

    ...


class Rrbracket(BaseElement):
    """⟧  related to `fourier mathclose` category."""

    ...


class Vert(BaseElement):
    """⦀  related to `fourier mathfence` category."""

    ...


class Iint(BaseElement):
    """∬  related to `fourier mathop` category."""

    ...


class Iiint(BaseElement):
    """∭  related to `fourier mathop` category."""

    ...


class Oiint(BaseElement):
    """∯  related to `fourier mathop` category."""

    ...


class Oiiint(BaseElement):
    """∰  related to `fourier mathop` category."""

    ...


class Llbracket(BaseElement):
    """⟦  related to `fourier mathopen` category."""

    ...


class Pounds(BaseElement):
    """£  related to `fourier mathord` category."""

    ...


class Complement(BaseElement):
    """∁  related to `fourier mathord` category."""

    ...


class Nexists(BaseElement):
    """∄  related to `fourier mathord` category."""

    ...


class Square(BaseElement):
    """◻  related to `fourier mathord` category."""

    ...


class Blacksquare(BaseElement):
    """◼  related to `fourier mathord` category."""

    ...


class Blacksquare(BaseElement):
    """⬛  related to `fourier mathord` category."""

    ...


class Square(BaseElement):
    """⬜  related to `fourier mathord` category."""

    ...


class Curvearrowleft(BaseElement):
    """↶  related to `fourier mathrel` category."""

    ...


class Curvearrowright(BaseElement):
    """↷  related to `fourier mathrel` category."""

    ...


class Leftleftarrows(BaseElement):
    """⇇  related to `fourier mathrel` category."""

    ...


class Rightrightarrows(BaseElement):
    """⇉  related to `fourier mathrel` category."""

    ...


class Nparallel(BaseElement):
    """∦  related to `fourier mathrel` category."""

    ...


class Vdash(BaseElement):
    """⊨  related to `fourier mathrel` category."""

    ...


class Nvdash(BaseElement):
    """⊭  related to `fourier mathrel` category."""

    ...


class Leqslant(BaseElement):
    """⩽  related to `fourier mathrel` category."""

    ...


class Geqslant(BaseElement):
    """⩾  related to `fourier mathrel` category."""

    ...


class A(BaseElement):
    """𝐴  related to `frenchstyle mathalpha` category."""

    ...


class B(BaseElement):
    """𝐵  related to `frenchstyle mathalpha` category."""

    ...


class C(BaseElement):
    """𝐶  related to `frenchstyle mathalpha` category."""

    ...


class D(BaseElement):
    """𝐷  related to `frenchstyle mathalpha` category."""

    ...


class E(BaseElement):
    """𝐸  related to `frenchstyle mathalpha` category."""

    ...


class F(BaseElement):
    """𝐹  related to `frenchstyle mathalpha` category."""

    ...


class G(BaseElement):
    """𝐺  related to `frenchstyle mathalpha` category."""

    ...


class H(BaseElement):
    """𝐻  related to `frenchstyle mathalpha` category."""

    ...


class I(BaseElement):
    """𝐼  related to `frenchstyle mathalpha` category."""

    ...


class J(BaseElement):
    """𝐽  related to `frenchstyle mathalpha` category."""

    ...


class K(BaseElement):
    """𝐾  related to `frenchstyle mathalpha` category."""

    ...


class L(BaseElement):
    """𝐿  related to `frenchstyle mathalpha` category."""

    ...


class M(BaseElement):
    """𝑀  related to `frenchstyle mathalpha` category."""

    ...


class N(BaseElement):
    """𝑁  related to `frenchstyle mathalpha` category."""

    ...


class O(BaseElement):
    """𝑂  related to `frenchstyle mathalpha` category."""

    ...


class P(BaseElement):
    """𝑃  related to `frenchstyle mathalpha` category."""

    ...


class Q(BaseElement):
    """𝑄  related to `frenchstyle mathalpha` category."""

    ...


class R(BaseElement):
    """𝑅  related to `frenchstyle mathalpha` category."""

    ...


class S(BaseElement):
    """𝑆  related to `frenchstyle mathalpha` category."""

    ...


class T(BaseElement):
    """𝑇  related to `frenchstyle mathalpha` category."""

    ...


class U(BaseElement):
    """𝑈  related to `frenchstyle mathalpha` category."""

    ...


class V(BaseElement):
    """𝑉  related to `frenchstyle mathalpha` category."""

    ...


class W(BaseElement):
    """𝑊  related to `frenchstyle mathalpha` category."""

    ...


class X(BaseElement):
    """𝑋  related to `frenchstyle mathalpha` category."""

    ...


class Y(BaseElement):
    """𝑌  related to `frenchstyle mathalpha` category."""

    ...


class Z(BaseElement):
    """𝑍  related to `frenchstyle mathalpha` category."""

    ...


class Rrbracket(BaseElement):
    """⟧  related to `kpfonts mathclose` category."""

    ...


class Llbracket(BaseElement):
    """⟦  related to `kpfonts mathopen` category."""

    ...


class A(BaseElement):
    """A  related to `literal mathalpha` category."""

    ...


class B(BaseElement):
    """B  related to `literal mathalpha` category."""

    ...


class C(BaseElement):
    """C  related to `literal mathalpha` category."""

    ...


class D(BaseElement):
    """D  related to `literal mathalpha` category."""

    ...


class E(BaseElement):
    """E  related to `literal mathalpha` category."""

    ...


class F(BaseElement):
    """F  related to `literal mathalpha` category."""

    ...


class G(BaseElement):
    """G  related to `literal mathalpha` category."""

    ...


class H(BaseElement):
    """H  related to `literal mathalpha` category."""

    ...


class I(BaseElement):
    """I  related to `literal mathalpha` category."""

    ...


class J(BaseElement):
    """J  related to `literal mathalpha` category."""

    ...


class K(BaseElement):
    """K  related to `literal mathalpha` category."""

    ...


class L(BaseElement):
    """L  related to `literal mathalpha` category."""

    ...


class M(BaseElement):
    """M  related to `literal mathalpha` category."""

    ...


class N(BaseElement):
    """N  related to `literal mathalpha` category."""

    ...


class O(BaseElement):
    """O  related to `literal mathalpha` category."""

    ...


class P(BaseElement):
    """P  related to `literal mathalpha` category."""

    ...


class Q(BaseElement):
    """Q  related to `literal mathalpha` category."""

    ...


class R(BaseElement):
    """R  related to `literal mathalpha` category."""

    ...


class S(BaseElement):
    """S  related to `literal mathalpha` category."""

    ...


class T(BaseElement):
    """T  related to `literal mathalpha` category."""

    ...


class U(BaseElement):
    """U  related to `literal mathalpha` category."""

    ...


class V(BaseElement):
    """V  related to `literal mathalpha` category."""

    ...


class W(BaseElement):
    """W  related to `literal mathalpha` category."""

    ...


class X(BaseElement):
    """X  related to `literal mathalpha` category."""

    ...


class Y(BaseElement):
    """Y  related to `literal mathalpha` category."""

    ...


class Z(BaseElement):
    """Z  related to `literal mathalpha` category."""

    ...


class A(BaseElement):
    """a  related to `literal mathalpha` category."""

    ...


class B(BaseElement):
    """b  related to `literal mathalpha` category."""

    ...


class C(BaseElement):
    """c  related to `literal mathalpha` category."""

    ...


class D(BaseElement):
    """d  related to `literal mathalpha` category."""

    ...


class E(BaseElement):
    """e  related to `literal mathalpha` category."""

    ...


class F(BaseElement):
    """f  related to `literal mathalpha` category."""

    ...


class G(BaseElement):
    """g  related to `literal mathalpha` category."""

    ...


class H(BaseElement):
    """h  related to `literal mathalpha` category."""

    ...


class I(BaseElement):
    """i  related to `literal mathalpha` category."""

    ...


class J(BaseElement):
    """j  related to `literal mathalpha` category."""

    ...


class K(BaseElement):
    """k  related to `literal mathalpha` category."""

    ...


class L(BaseElement):
    """l  related to `literal mathalpha` category."""

    ...


class M(BaseElement):
    """m  related to `literal mathalpha` category."""

    ...


class N(BaseElement):
    """n  related to `literal mathalpha` category."""

    ...


class O(BaseElement):
    """o  related to `literal mathalpha` category."""

    ...


class P(BaseElement):
    """p  related to `literal mathalpha` category."""

    ...


class Q(BaseElement):
    """q  related to `literal mathalpha` category."""

    ...


class R(BaseElement):
    """r  related to `literal mathalpha` category."""

    ...


class S(BaseElement):
    """s  related to `literal mathalpha` category."""

    ...


class T(BaseElement):
    """t  related to `literal mathalpha` category."""

    ...


class U(BaseElement):
    """u  related to `literal mathalpha` category."""

    ...


class V(BaseElement):
    """v  related to `literal mathalpha` category."""

    ...


class W(BaseElement):
    """w  related to `literal mathalpha` category."""

    ...


class X(BaseElement):
    """x  related to `literal mathalpha` category."""

    ...


class Y(BaseElement):
    """y  related to `literal mathalpha` category."""

    ...


class Z(BaseElement):
    """z  related to `literal mathalpha` category."""

    ...


class Imath(BaseElement):
    """ı  related to `literal mathalpha` category."""

    ...


class Jmath(BaseElement):
    """ȷ  related to `literal mathalpha` category."""

    ...


class Gamma(BaseElement):
    """Γ  related to `literal mathalpha` category."""

    ...


class Delta(BaseElement):
    """Δ  related to `literal mathalpha` category."""

    ...


class Theta(BaseElement):
    """Θ  related to `literal mathalpha` category."""

    ...


class Lambda(BaseElement):
    """Λ  related to `literal mathalpha` category."""

    ...


class Xi(BaseElement):
    """Ξ  related to `literal mathalpha` category."""

    ...


class Pi(BaseElement):
    """Π  related to `literal mathalpha` category."""

    ...


class Sigma(BaseElement):
    """Σ  related to `literal mathalpha` category."""

    ...


class Upsilon(BaseElement):
    """Υ  related to `literal mathalpha` category."""

    ...


class Phi(BaseElement):
    """Φ  related to `literal mathalpha` category."""

    ...


class Psi(BaseElement):
    """Ψ  related to `literal mathalpha` category."""

    ...


class Omega(BaseElement):
    """Ω  related to `literal mathalpha` category."""

    ...


class Alpha(BaseElement):
    """α  related to `literal mathalpha` category."""

    ...


class Beta(BaseElement):
    """β  related to `literal mathalpha` category."""

    ...


class Gamma(BaseElement):
    """γ  related to `literal mathalpha` category."""

    ...


class Delta(BaseElement):
    """δ  related to `literal mathalpha` category."""

    ...


class Varepsilon(BaseElement):
    """ε  related to `literal mathalpha` category."""

    ...


class Zeta(BaseElement):
    """ζ  related to `literal mathalpha` category."""

    ...


class Eta(BaseElement):
    """η  related to `literal mathalpha` category."""

    ...


class Theta(BaseElement):
    """θ  related to `literal mathalpha` category."""

    ...


class Iota(BaseElement):
    """ι  related to `literal mathalpha` category."""

    ...


class Kappa(BaseElement):
    """κ  related to `literal mathalpha` category."""

    ...


class Lambda(BaseElement):
    """λ  related to `literal mathalpha` category."""

    ...


class Mu(BaseElement):
    """μ  related to `literal mathalpha` category."""

    ...


class Nu(BaseElement):
    """ν  related to `literal mathalpha` category."""

    ...


class Xi(BaseElement):
    """ξ  related to `literal mathalpha` category."""

    ...


class Pi(BaseElement):
    """π  related to `literal mathalpha` category."""

    ...


class Rho(BaseElement):
    """ρ  related to `literal mathalpha` category."""

    ...


class Varsigma(BaseElement):
    """ς  related to `literal mathalpha` category."""

    ...


class Sigma(BaseElement):
    """σ  related to `literal mathalpha` category."""

    ...


class Tau(BaseElement):
    """τ  related to `literal mathalpha` category."""

    ...


class Upsilon(BaseElement):
    """υ  related to `literal mathalpha` category."""

    ...


class Varphi(BaseElement):
    """φ  related to `literal mathalpha` category."""

    ...


class Chi(BaseElement):
    """χ  related to `literal mathalpha` category."""

    ...


class Psi(BaseElement):
    """ψ  related to `literal mathalpha` category."""

    ...


class Omega(BaseElement):
    """ω  related to `literal mathalpha` category."""

    ...


class Vartheta(BaseElement):
    """ϑ  related to `literal mathalpha` category."""

    ...


class Phi(BaseElement):
    """ϕ  related to `literal mathalpha` category."""

    ...


class Varpi(BaseElement):
    """ϖ  related to `literal mathalpha` category."""

    ...


class Varrho(BaseElement):
    """ϱ  related to `literal mathalpha` category."""

    ...


class Epsilon(BaseElement):
    """ϵ  related to `literal mathalpha` category."""

    ...


class Partial(BaseElement):
    """∂  related to `literal mathord` category."""

    ...


# class :(BaseElement):
#     """  :  related to `literal mathpunct` category.
#     """
#     ...


class Rightarrow(BaseElement):
    """⇒  related to `marvosym mathrel` category."""

    ...


class Blacktriangleup(BaseElement):
    """▴  related to `mathabx mathbin` category."""

    ...


class Smalltriangleup(BaseElement):
    """▵  related to `mathabx mathbin` category."""

    ...


class Blacktriangleright(BaseElement):
    """▸  related to `mathabx mathbin` category."""

    ...


class Smalltriangleright(BaseElement):
    """▹  related to `mathabx mathbin` category."""

    ...


class Blacktriangledown(BaseElement):
    """▾  related to `mathabx mathbin` category."""

    ...


class Smalltriangledown(BaseElement):
    """▿  related to `mathabx mathbin` category."""

    ...


class Blacktriangleleft(BaseElement):
    """◂  related to `mathabx mathbin` category."""

    ...


class Smalltriangleleft(BaseElement):
    """◃  related to `mathabx mathbin` category."""

    ...


class Second(BaseElement):
    """″  related to `mathabx mathord` category."""

    ...


class Third(BaseElement):
    """‴  related to `mathabx mathord` category."""

    ...


class Fourth(BaseElement):
    """⁗  related to `mathabx mathord` category."""

    ...


class Diameter(BaseElement):
    """⌀  related to `mathabx mathord` category."""

    ...


class Sun(BaseElement):
    """☉  related to `mathabx mathord` category."""

    ...


class Rightmoon(BaseElement):
    """☽  related to `mathabx mathord` category."""

    ...


class Leftmoon(BaseElement):
    """☾  related to `mathabx mathord` category."""

    ...


class Dlsh(BaseElement):
    """↲  related to `mathabx mathrel` category."""

    ...


class Drsh(BaseElement):
    """↳  related to `mathabx mathrel` category."""

    ...


class Updownarrows(BaseElement):
    """⇅  related to `mathabx mathrel` category."""

    ...


class Leftsquigarrow(BaseElement):
    """⇜  related to `mathabx mathrel` category."""

    ...


class Downuparrows(BaseElement):
    """⇵  related to `mathabx mathrel` category."""

    ...


class Eqcolon(BaseElement):
    """∹  related to `mathabx mathrel` category."""

    ...


class Coloneq(BaseElement):
    """≔  related to `mathabx mathrel` category."""

    ...


class Eqcolon(BaseElement):
    """≕  related to `mathabx mathrel` category."""

    ...


class Corresponds(BaseElement):
    """≙  related to `mathabx mathrel` category."""

    ...


class Notasymp(BaseElement):
    """≭  related to `mathabx mathrel` category."""

    ...


class Vdash(BaseElement):
    """⊫  related to `mathabx mathrel` category."""

    ...


class Hash(BaseElement):
    """⋕  related to `mathabx mathrel` category."""

    ...


class Lll(BaseElement):
    """⋘  related to `mathabx mathrel` category."""

    ...


class Ggg(BaseElement):
    """⋙  related to `mathabx mathrel` category."""

    ...


class Barin(BaseElement):
    """⋶  related to `mathabx mathrel` category."""

    ...


class Leftrightharpoon(BaseElement):
    """⥊  related to `mathabx mathrel` category."""

    ...


class Rightleftharpoon(BaseElement):
    """⥋  related to `mathabx mathrel` category."""

    ...


class Leftleftharpoons(BaseElement):
    """⥢  related to `mathabx mathrel` category."""

    ...


class Upupharpoons(BaseElement):
    """⥣  related to `mathabx mathrel` category."""

    ...


class Rightrightharpoons(BaseElement):
    """⥤  related to `mathabx mathrel` category."""

    ...


class Downdownharpoons(BaseElement):
    """⥥  related to `mathabx mathrel` category."""

    ...


class Leftbarharpoon(BaseElement):
    """⥪  related to `mathabx mathrel` category."""

    ...


class Barleftharpoon(BaseElement):
    """⥫  related to `mathabx mathrel` category."""

    ...


class Rightbarharpoon(BaseElement):
    """⥬  related to `mathabx mathrel` category."""

    ...


class Barrightharpoon(BaseElement):
    """⥭  related to `mathabx mathrel` category."""

    ...


class Updownharpoons(BaseElement):
    """⥮  related to `mathabx mathrel` category."""

    ...


class Downupharpoons(BaseElement):
    """⥯  related to `mathabx mathrel` category."""

    ...


class Llcurly(BaseElement):
    """⪻  related to `mathabx mathrel` category."""

    ...


class Ggcurly(BaseElement):
    """⪼  related to `mathabx mathrel` category."""

    ...


class Rparen(BaseElement):
    """⦆  related to `mathbbol mathclose` category."""

    ...


class Lparen(BaseElement):
    """⦅  related to `mathbbol mathopen` category."""

    ...


class Tcohm(BaseElement):
    """Ω  related to `mathcomp mathalpha` category."""

    ...


class Iddots(BaseElement):
    """⋰  related to `mathdots mathrel` category."""

    ...


class Pounds(BaseElement):
    """£  related to `omlmathit mathord` category."""

    ...


class Dot(BaseElement):
    """̇  related to `oz mathaccent` category."""

    ...


class Cat(BaseElement):
    """⁀  related to `oz mathbin` category."""

    ...


class Fcmp(BaseElement):
    """⨾  related to `oz mathbin` category."""

    ...


class Dsub(BaseElement):
    """⩤  related to `oz mathbin` category."""

    ...


class Rsub(BaseElement):
    """⩥  related to `oz mathbin` category."""

    ...


class Rang(BaseElement):
    """⟫  related to `oz mathclose` category."""

    ...


class Rimg(BaseElement):
    """⦈  related to `oz mathclose` category."""

    ...


class Rblot(BaseElement):
    """⦊  related to `oz mathclose` category."""

    ...


class Zhide(BaseElement):
    """⧹  related to `oz mathop` category."""

    ...


class Zcmp(BaseElement):
    """⨟  related to `oz mathop` category."""

    ...


class Zpipe(BaseElement):
    """⨠  related to `oz mathop` category."""

    ...


class Zproject(BaseElement):
    """⨡  related to `oz mathop` category."""

    ...


class Lang(BaseElement):
    """⟪  related to `oz mathopen` category."""

    ...


class Limg(BaseElement):
    """⦇  related to `oz mathopen` category."""

    ...


class Lblot(BaseElement):
    """⦉  related to `oz mathopen` category."""

    ...


# class #(BaseElement):
#     """  #  related to `oz mathord` category.
#     """
#     ...


class Spot(BaseElement):
    """⦁  related to `oz mathord` category."""

    ...


class Pfun(BaseElement):
    """⇸  related to `oz mathrel` category."""

    ...


class Ffun(BaseElement):
    """⇻  related to `oz mathrel` category."""

    ...


class Psur(BaseElement):
    """⤀  related to `oz mathrel` category."""

    ...


class Pinj(BaseElement):
    """⤔  related to `oz mathrel` category."""

    ...


class Finj(BaseElement):
    """⤕  related to `oz mathrel` category."""

    ...


class Bij(BaseElement):
    """⤖  related to `oz mathrel` category."""

    ...


class Gamma(BaseElement):
    """𝛤  related to `slantedGreek mathalpha` category."""

    ...


class Delta(BaseElement):
    """𝛥  related to `slantedGreek mathalpha` category."""

    ...


class Theta(BaseElement):
    """𝛩  related to `slantedGreek mathalpha` category."""

    ...


class Lambda(BaseElement):
    """𝛬  related to `slantedGreek mathalpha` category."""

    ...


class Xi(BaseElement):
    """𝛯  related to `slantedGreek mathalpha` category."""

    ...


class Pi(BaseElement):
    """𝛱  related to `slantedGreek mathalpha` category."""

    ...


class Sigma(BaseElement):
    """𝛴  related to `slantedGreek mathalpha` category."""

    ...


class Upsilon(BaseElement):
    """𝛶  related to `slantedGreek mathalpha` category."""

    ...


class Phi(BaseElement):
    """𝛷  related to `slantedGreek mathalpha` category."""

    ...


class Psi(BaseElement):
    """𝛹  related to `slantedGreek mathalpha` category."""

    ...


class Omega(BaseElement):
    """𝛺  related to `slantedGreek mathalpha` category."""

    ...


class Boxdot(BaseElement):
    """⊡  related to `stmaryrd mathbin` category."""

    ...


class Bigtriangleup(BaseElement):
    """△  related to `stmaryrd mathbin` category."""

    ...


class Bigtriangledown(BaseElement):
    """▽  related to `stmaryrd mathbin` category."""

    ...


class Boxbar(BaseElement):
    """◫  related to `stmaryrd mathbin` category."""

    ...


class Boxslash(BaseElement):
    """⧄  related to `stmaryrd mathbin` category."""

    ...


class Boxbslash(BaseElement):
    """⧅  related to `stmaryrd mathbin` category."""

    ...


class Boxast(BaseElement):
    """⧆  related to `stmaryrd mathbin` category."""

    ...


class Boxcircle(BaseElement):
    """⧇  related to `stmaryrd mathbin` category."""

    ...


class Boxbox(BaseElement):
    """⧈  related to `stmaryrd mathbin` category."""

    ...


class Interleave(BaseElement):
    """⫴  related to `stmaryrd mathbin` category."""

    ...


class Sslash(BaseElement):
    """⫽  related to `stmaryrd mathbin` category."""

    ...


class Talloblong(BaseElement):
    """⫾  related to `stmaryrd mathbin` category."""

    ...


class Rbag(BaseElement):
    """⟆  related to `stmaryrd mathclose` category."""

    ...


class Rrbracket(BaseElement):
    """⟧  related to `stmaryrd mathclose` category."""

    ...


class Biginterleave(BaseElement):
    """⫼  related to `stmaryrd mathop` category."""

    ...


class Lbag(BaseElement):
    """⟅  related to `stmaryrd mathopen` category."""

    ...


class Llbracket(BaseElement):
    """⟦  related to `stmaryrd mathopen` category."""

    ...


class Yup(BaseElement):
    """⅄  related to `stmaryrd mathord` category."""

    ...


class Mapsfrom(BaseElement):
    """↤  related to `stmaryrd mathrel` category."""

    ...


class Lightning(BaseElement):
    """↯  related to `stmaryrd mathrel` category."""

    ...


class Leftarrowtriangle(BaseElement):
    """⇽  related to `stmaryrd mathrel` category."""

    ...


class Rightarrowtriangle(BaseElement):
    """⇾  related to `stmaryrd mathrel` category."""

    ...


class Leftrightarrowtriangle(BaseElement):
    """⇿  related to `stmaryrd mathrel` category."""

    ...


class Longmapsfrom(BaseElement):
    """⟻  related to `stmaryrd mathrel` category."""

    ...


class Longmapsfrom(BaseElement):
    """⟽  related to `stmaryrd mathrel` category."""

    ...


class Longmapsto(BaseElement):
    """⟾  related to `stmaryrd mathrel` category."""

    ...


class Mapsfrom(BaseElement):
    """⤆  related to `stmaryrd mathrel` category."""

    ...


class Mapsto(BaseElement):
    """⤇  related to `stmaryrd mathrel` category."""

    ...


class Leftslice(BaseElement):
    """⪦  related to `stmaryrd mathrel` category."""

    ...


class Rightslice(BaseElement):
    """⪧  related to `stmaryrd mathrel` category."""

    ...


class Invamp(BaseElement):
    """⅋  related to `txfonts mathbin` category."""

    ...


class Boxbar(BaseElement):
    """◫  related to `txfonts mathbin` category."""

    ...


class Circledbslash(BaseElement):
    """⦸  related to `txfonts mathbin` category."""

    ...


class Circledless(BaseElement):
    """⧀  related to `txfonts mathbin` category."""

    ...


class Circledgtr(BaseElement):
    """⧁  related to `txfonts mathbin` category."""

    ...


class Boxslash(BaseElement):
    """⧄  related to `txfonts mathbin` category."""

    ...


class Boxbslash(BaseElement):
    """⧅  related to `txfonts mathbin` category."""

    ...


class Boxast(BaseElement):
    """⧆  related to `txfonts mathbin` category."""

    ...


class Rbag(BaseElement):
    """⟆  related to `txfonts mathclose` category."""

    ...


class Oiiint(BaseElement):
    """∰  related to `txfonts mathop` category."""

    ...


class Bigsqcap(BaseElement):
    """⨅  related to `txfonts mathop` category."""

    ...


class Varprod(BaseElement):
    """⨉  related to `txfonts mathop` category."""

    ...


class Lbag(BaseElement):
    """⟅  related to `txfonts mathopen` category."""

    ...


class Diamondblack(BaseElement):
    """◆  related to `txfonts mathord` category."""

    ...


class Varspadesuit(BaseElement):
    """♤  related to `txfonts mathord` category."""

    ...


class Varheartsuit(BaseElement):
    """♥  related to `txfonts mathord` category."""

    ...


class Vardiamondsuit(BaseElement):
    """♦  related to `txfonts mathord` category."""

    ...


class Varclubsuit(BaseElement):
    """♧  related to `txfonts mathord` category."""

    ...


class Medcirc(BaseElement):
    """⚪  related to `txfonts mathord` category."""

    ...


class Medbullet(BaseElement):
    """⚫  related to `txfonts mathord` category."""

    ...


class Diamonddot(BaseElement):
    """⟐  related to `txfonts mathord` category."""

    ...


class Nwarrow(BaseElement):
    """⇖  related to `txfonts mathrel` category."""

    ...


class Nearrow(BaseElement):
    """⇗  related to `txfonts mathrel` category."""

    ...


class Searrow(BaseElement):
    """⇘  related to `txfonts mathrel` category."""

    ...


class Swarrow(BaseElement):
    """⇙  related to `txfonts mathrel` category."""

    ...


class Leftsquigarrow(BaseElement):
    """⇜  related to `txfonts mathrel` category."""

    ...


class Eqcolon(BaseElement):
    """∹  related to `txfonts mathrel` category."""

    ...


class Nsimeq(BaseElement):
    """≄  related to `txfonts mathrel` category."""

    ...


class Coloneq(BaseElement):
    """≔  related to `txfonts mathrel` category."""

    ...


class Eqcolon(BaseElement):
    """≕  related to `txfonts mathrel` category."""

    ...


class Vdash(BaseElement):
    """⊫  related to `txfonts mathrel` category."""

    ...


class Multimapdotbotha(BaseElement):
    """⊶  related to `txfonts mathrel` category."""

    ...


class Multimapdotbothb(BaseElement):
    """⊷  related to `txfonts mathrel` category."""

    ...


class Multimapinv(BaseElement):
    """⟜  related to `txfonts mathrel` category."""

    ...


class Leadsto(BaseElement):
    """⤳  related to `txfonts mathrel` category."""

    ...


class Strictfi(BaseElement):
    """⥼  related to `txfonts mathrel` category."""

    ...


class Strictif(BaseElement):
    """⥽  related to `txfonts mathrel` category."""

    ...


class Multimapboth(BaseElement):
    """⧟  related to `txfonts mathrel` category."""

    ...


class Coloneqq(BaseElement):
    """⩴  related to `txfonts mathrel` category."""

    ...


class Preceqq(BaseElement):
    """⪳  related to `txfonts mathrel` category."""

    ...


class Succeqq(BaseElement):
    """⪴  related to `txfonts mathrel` category."""

    ...


class Top(BaseElement):
    """⫪  related to `txfonts mathrel` category."""

    ...


class Bot(BaseElement):
    """⫫  related to `txfonts mathrel` category."""

    ...


class Utilde(BaseElement):
    """̰  related to `undertilde mathaccent` category."""

    ...


class A(BaseElement):
    """𝑎  related to `uprightstyle mathalpha` category."""

    ...


class B(BaseElement):
    """𝑏  related to `uprightstyle mathalpha` category."""

    ...


class C(BaseElement):
    """𝑐  related to `uprightstyle mathalpha` category."""

    ...


class D(BaseElement):
    """𝑑  related to `uprightstyle mathalpha` category."""

    ...


class E(BaseElement):
    """𝑒  related to `uprightstyle mathalpha` category."""

    ...


class F(BaseElement):
    """𝑓  related to `uprightstyle mathalpha` category."""

    ...


class G(BaseElement):
    """𝑔  related to `uprightstyle mathalpha` category."""

    ...


class I(BaseElement):
    """𝑖  related to `uprightstyle mathalpha` category."""

    ...


class J(BaseElement):
    """𝑗  related to `uprightstyle mathalpha` category."""

    ...


class K(BaseElement):
    """𝑘  related to `uprightstyle mathalpha` category."""

    ...


class L(BaseElement):
    """𝑙  related to `uprightstyle mathalpha` category."""

    ...


class M(BaseElement):
    """𝑚  related to `uprightstyle mathalpha` category."""

    ...


class N(BaseElement):
    """𝑛  related to `uprightstyle mathalpha` category."""

    ...


class O(BaseElement):
    """𝑜  related to `uprightstyle mathalpha` category."""

    ...


class P(BaseElement):
    """𝑝  related to `uprightstyle mathalpha` category."""

    ...


class Q(BaseElement):
    """𝑞  related to `uprightstyle mathalpha` category."""

    ...


class R(BaseElement):
    """𝑟  related to `uprightstyle mathalpha` category."""

    ...


class S(BaseElement):
    """𝑠  related to `uprightstyle mathalpha` category."""

    ...


class T(BaseElement):
    """𝑡  related to `uprightstyle mathalpha` category."""

    ...


class U(BaseElement):
    """𝑢  related to `uprightstyle mathalpha` category."""

    ...


class V(BaseElement):
    """𝑣  related to `uprightstyle mathalpha` category."""

    ...


class W(BaseElement):
    """𝑤  related to `uprightstyle mathalpha` category."""

    ...


class X(BaseElement):
    """𝑥  related to `uprightstyle mathalpha` category."""

    ...


class Y(BaseElement):
    """𝑦  related to `uprightstyle mathalpha` category."""

    ...


class Z(BaseElement):
    """𝑧  related to `uprightstyle mathalpha` category."""

    ...


class Rhd(BaseElement):
    """▶  related to `wasysym mathbin` category."""

    ...


class Rhd(BaseElement):
    """▷  related to `wasysym mathbin` category."""

    ...


class Lhd(BaseElement):
    """◀  related to `wasysym mathbin` category."""

    ...


class Lhd(BaseElement):
    """◁  related to `wasysym mathbin` category."""

    ...


class Circle(BaseElement):
    """○  related to `wasysym mathbin` category."""

    ...


class Iint(BaseElement):
    """∬  related to `wasysym mathop` category."""

    ...


class Iiint(BaseElement):
    """∭  related to `wasysym mathop` category."""

    ...


class Oiint(BaseElement):
    """∯  related to `wasysym mathop` category."""

    ...


class Cent(BaseElement):
    """¢  related to `wasysym mathord` category."""

    ...


class Ac(BaseElement):
    """∿  related to `wasysym mathord` category."""

    ...


class Invneg(BaseElement):
    """⌐  related to `wasysym mathord` category."""

    ...


class Wasylozenge(BaseElement):
    """⌑  related to `wasysym mathord` category."""

    ...


class Aplinv(BaseElement):
    """⌹  related to `wasysym mathord` category."""

    ...


class Notbackslash(BaseElement):
    """⍀  related to `wasysym mathord` category."""

    ...


class Aplleftarrowbox(BaseElement):
    """⍇  related to `wasysym mathord` category."""

    ...


class Aplrightarrowbox(BaseElement):
    """⍈  related to `wasysym mathord` category."""

    ...


class Apluparrowbox(BaseElement):
    """⍐  related to `wasysym mathord` category."""

    ...


class Apldownarrowbox(BaseElement):
    """⍗  related to `wasysym mathord` category."""

    ...


class Aplcomment(BaseElement):
    """⍝  related to `wasysym mathord` category."""

    ...


class AplInput(BaseElement):
    """⍞  related to `wasysym mathord` category."""

    ...


class Apllog(BaseElement):
    """⍟  related to `wasysym mathord` category."""

    ...


class Circle(BaseElement):
    """●  related to `wasysym mathord` category."""

    ...


class Leftcircle(BaseElement):
    """◐  related to `wasysym mathord` category."""

    ...


class Rightcircle(BaseElement):
    """◑  related to `wasysym mathord` category."""

    ...


class Leftcircle(BaseElement):
    """◖  related to `wasysym mathord` category."""

    ...


class Rightcircle(BaseElement):
    """◗  related to `wasysym mathord` category."""

    ...


class Square(BaseElement):
    """☐  related to `wasysym mathord` category."""

    ...


class Checkedbox(BaseElement):
    """☑  related to `wasysym mathord` category."""

    ...


class Xbox(BaseElement):
    """☒  related to `wasysym mathord` category."""

    ...


class Frownie(BaseElement):
    """☹  related to `wasysym mathord` category."""

    ...


class Smiley(BaseElement):
    """☺  related to `wasysym mathord` category."""

    ...


class Blacksmiley(BaseElement):
    """☻  related to `wasysym mathord` category."""

    ...


class Sun(BaseElement):
    """☼  related to `wasysym mathord` category."""

    ...


class Rightmoon(BaseElement):
    """☽  related to `wasysym mathord` category."""

    ...


class Leftmoon(BaseElement):
    """☾  related to `wasysym mathord` category."""

    ...


class Mercury(BaseElement):
    """☿  related to `wasysym mathord` category."""

    ...


class Female(BaseElement):
    """♀  related to `wasysym mathord` category."""

    ...


class Earth(BaseElement):
    """♁  related to `wasysym mathord` category."""

    ...


class Male(BaseElement):
    """♂  related to `wasysym mathord` category."""

    ...


class Jupiter(BaseElement):
    """♃  related to `wasysym mathord` category."""

    ...


class Saturn(BaseElement):
    """♄  related to `wasysym mathord` category."""

    ...


class Uranus(BaseElement):
    """♅  related to `wasysym mathord` category."""

    ...


class Neptune(BaseElement):
    """♆  related to `wasysym mathord` category."""

    ...


class Pluto(BaseElement):
    """♇  related to `wasysym mathord` category."""

    ...


class Aries(BaseElement):
    """♈  related to `wasysym mathord` category."""

    ...


class Taurus(BaseElement):
    """♉  related to `wasysym mathord` category."""

    ...


class Gemini(BaseElement):
    """♊  related to `wasysym mathord` category."""

    ...


class Cancer(BaseElement):
    """♋  related to `wasysym mathord` category."""

    ...


class Leo(BaseElement):
    """♌  related to `wasysym mathord` category."""

    ...


class Virgo(BaseElement):
    """♍  related to `wasysym mathord` category."""

    ...


class Libra(BaseElement):
    """♎  related to `wasysym mathord` category."""

    ...


class Scorpio(BaseElement):
    """♏  related to `wasysym mathord` category."""

    ...


class Sagittarius(BaseElement):
    """♐  related to `wasysym mathord` category."""

    ...


class Capricornus(BaseElement):
    """♑  related to `wasysym mathord` category."""

    ...


class Aquarius(BaseElement):
    """♒  related to `wasysym mathord` category."""

    ...


class Pisces(BaseElement):
    """♓  related to `wasysym mathord` category."""

    ...


class Quarternote(BaseElement):
    """♩  related to `wasysym mathord` category."""

    ...


class Twonotes(BaseElement):
    """♫  related to `wasysym mathord` category."""

    ...


class Notslash(BaseElement):
    """⌿  related to `wasysym mathrel` category."""

    ...


class Lvec(BaseElement):
    """⃐  related to `wrisym mathaccent` category."""

    ...


class Vec(BaseElement):
    """⃑  related to `wrisym mathaccent` category."""

    ...


class Lvec(BaseElement):
    """⃖  related to `wrisym mathaccent` category."""

    ...


class Vec(BaseElement):
    """⃗  related to `wrisym mathaccent` category."""

    ...


class Micro(BaseElement):
    """µ  related to `wrisym mathalpha` category."""

    ...


class Stigma(BaseElement):
    """Ϛ  related to `wrisym mathalpha` category."""

    ...


class Stigma(BaseElement):
    """ϛ  related to `wrisym mathalpha` category."""

    ...


class Digamma(BaseElement):
    """Ϝ  related to `wrisym mathalpha` category."""

    ...


class Digamma(BaseElement):
    """ϝ  related to `wrisym mathalpha` category."""

    ...


class Sampi(BaseElement):
    """Ϡ  related to `wrisym mathalpha` category."""

    ...


class Angstroem(BaseElement):
    """Å  related to `wrisym mathalpha` category."""

    ...


class Beth(BaseElement):
    """ℶ  related to `wrisym mathalpha` category."""

    ...


class Gimel(BaseElement):
    """ℷ  related to `wrisym mathalpha` category."""

    ...


class Daleth(BaseElement):
    """ℸ  related to `wrisym mathalpha` category."""

    ...


class Rrbracket(BaseElement):
    """⟧  related to `wrisym mathclose` category."""

    ...


class Fint(BaseElement):
    """⨏  related to `wrisym mathop` category."""

    ...


class Llbracket(BaseElement):
    """⟦  related to `wrisym mathopen` category."""

    ...


class Backepsilon(BaseElement):
    """϶  related to `wrisym mathord` category."""

    ...


class Euler(BaseElement):
    """ℇ  related to `wrisym mathord` category."""

    ...


class Capitaldifferentiald(BaseElement):
    """ⅅ  related to `wrisym mathord` category."""

    ...


class Differentiald(BaseElement):
    """ⅆ  related to `wrisym mathord` category."""

    ...


class Exponetiale(BaseElement):
    """ⅇ  related to `wrisym mathord` category."""

    ...


class Complexi(BaseElement):
    """ⅈ  related to `wrisym mathord` category."""

    ...


class Complexj(BaseElement):
    """ⅉ  related to `wrisym mathord` category."""

    ...


class Rightangle(BaseElement):
    """∟  related to `wrisym mathord` category."""

    ...


class Measuredangle(BaseElement):
    """∡  related to `wrisym mathord` category."""

    ...


class Sphericalangle(BaseElement):
    """∢  related to `wrisym mathord` category."""

    ...


class Therefore(BaseElement):
    """∴  related to `wrisym mathord` category."""

    ...


class Because(BaseElement):
    """∵  related to `wrisym mathord` category."""

    ...


class Overparen(BaseElement):
    """⏜  related to `wrisym mathover` category."""

    ...


class Leftrightarrow(BaseElement):
    """↔  related to `wrisym mathrel` category."""

    ...


class Mapsup(BaseElement):
    """↥  related to `wrisym mathrel` category."""

    ...


class Mapsdown(BaseElement):
    """↧  related to `wrisym mathrel` category."""

    ...


class Leftarrowbar(BaseElement):
    """⇤  related to `wrisym mathrel` category."""

    ...


class Rightarrowbar(BaseElement):
    """⇥  related to `wrisym mathrel` category."""

    ...


class Nni(BaseElement):
    """∌  related to `wrisym mathrel` category."""

    ...


class Proportion(BaseElement):
    """∷  related to `wrisym mathrel` category."""

    ...


class Nsim(BaseElement):
    """≁  related to `wrisym mathrel` category."""

    ...


class Ncong(BaseElement):
    """≇  related to `wrisym mathrel` category."""

    ...


class Napprox(BaseElement):
    """≉  related to `wrisym mathrel` category."""

    ...


class Bumpeq(BaseElement):
    """≎  related to `wrisym mathrel` category."""

    ...


class Bumpeq(BaseElement):
    """≏  related to `wrisym mathrel` category."""

    ...


class Nequiv(BaseElement):
    """≢  related to `wrisym mathrel` category."""

    ...


class Nleq(BaseElement):
    """≰  related to `wrisym mathrel` category."""

    ...


class Ngeq(BaseElement):
    """≱  related to `wrisym mathrel` category."""

    ...


class Notlesstilde(BaseElement):
    """≴  related to `wrisym mathrel` category."""

    ...


class Notgreatertilde(BaseElement):
    """≵  related to `wrisym mathrel` category."""

    ...


class Notgreaterless(BaseElement):
    """≹  related to `wrisym mathrel` category."""

    ...


class Nprec(BaseElement):
    """⊀  related to `wrisym mathrel` category."""

    ...


class Nsucc(BaseElement):
    """⊁  related to `wrisym mathrel` category."""

    ...


class Nsubset(BaseElement):
    """⊄  related to `wrisym mathrel` category."""

    ...


class Nsupset(BaseElement):
    """⊅  related to `wrisym mathrel` category."""

    ...


class Nsubseteq(BaseElement):
    """⊈  related to `wrisym mathrel` category."""

    ...


class Nsupseteq(BaseElement):
    """⊉  related to `wrisym mathrel` category."""

    ...


class Npreceq(BaseElement):
    """⋠  related to `wrisym mathrel` category."""

    ...


class Nsucceq(BaseElement):
    """⋡  related to `wrisym mathrel` category."""

    ...


class Nsqsubseteq(BaseElement):
    """⋢  related to `wrisym mathrel` category."""

    ...


class Nsqsupseteq(BaseElement):
    """⋣  related to `wrisym mathrel` category."""

    ...


class Uparrowbar(BaseElement):
    """⤒  related to `wrisym mathrel` category."""

    ...


class Downarrowbar(BaseElement):
    """⤓  related to `wrisym mathrel` category."""

    ...


class Leftrightharpoonup(BaseElement):
    """⥎  related to `wrisym mathrel` category."""

    ...


class Rightupdownharpoon(BaseElement):
    """⥏  related to `wrisym mathrel` category."""

    ...


class Leftrightharpoondown(BaseElement):
    """⥐  related to `wrisym mathrel` category."""

    ...


class Leftupdownharpoon(BaseElement):
    """⥑  related to `wrisym mathrel` category."""

    ...


class Leftvectorbar(BaseElement):
    """⥒  related to `wrisym mathrel` category."""

    ...


class Rightvectorbar(BaseElement):
    """⥓  related to `wrisym mathrel` category."""

    ...


class Rightupvectorbar(BaseElement):
    """⥔  related to `wrisym mathrel` category."""

    ...


class Rightdownvectorbar(BaseElement):
    """⥕  related to `wrisym mathrel` category."""

    ...


class Downleftvectorbar(BaseElement):
    """⥖  related to `wrisym mathrel` category."""

    ...


class Downrightvectorbar(BaseElement):
    """⥗  related to `wrisym mathrel` category."""

    ...


class Leftupvectorbar(BaseElement):
    """⥘  related to `wrisym mathrel` category."""

    ...


class Leftdownvectorbar(BaseElement):
    """⥙  related to `wrisym mathrel` category."""

    ...


class Leftteevector(BaseElement):
    """⥚  related to `wrisym mathrel` category."""

    ...


class Rightteevector(BaseElement):
    """⥛  related to `wrisym mathrel` category."""

    ...


class Rightupteevector(BaseElement):
    """⥜  related to `wrisym mathrel` category."""

    ...


class Rightdownteevector(BaseElement):
    """⥝  related to `wrisym mathrel` category."""

    ...


class Downleftteevector(BaseElement):
    """⥞  related to `wrisym mathrel` category."""

    ...


class Downrightteevector(BaseElement):
    """⥟  related to `wrisym mathrel` category."""

    ...


class Leftupteevector(BaseElement):
    """⥠  related to `wrisym mathrel` category."""

    ...


class Leftdownteevector(BaseElement):
    """⥡  related to `wrisym mathrel` category."""

    ...


class Lefttrianglebar(BaseElement):
    """⧏  related to `wrisym mathrel` category."""

    ...


class Righttrianglebar(BaseElement):
    """⧐  related to `wrisym mathrel` category."""

    ...


class Equal(BaseElement):
    """⩵  related to `wrisym mathrel` category."""

    ...


class Same(BaseElement):
    """⩶  related to `wrisym mathrel` category."""

    ...


class Nestedlessless(BaseElement):
    """⪡  related to `wrisym mathrel` category."""

    ...


class Nestedgreatergreater(BaseElement):
    """⪢  related to `wrisym mathrel` category."""

    ...


class Underparen(BaseElement):
    """⏝  related to `wrisym mathunder` category."""

    ...
